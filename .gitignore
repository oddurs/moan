/target

# The archive and anything it strands beside itself. Never in the repo: it is
# a copy of your transcripts.
*.db
*.db-wal
*.db-shm

# Whatever a local run leaves behind.
/dist
*.profraw

.DS_Store
