# Roadmap

<!-- generated by cairn; edit the files in `cairn/items` and run `cairn render` -->

## v0.1 — reads Claude Code

`##########` 100% · 10 of 10 done · due 2026-09-14

Point it at a Claude Code session and read what happened as a compressed feed — one tight line per event, newest at the bottom, expandable to the original text.

### done

- [x] `0010` Parse Claude Code transcripts into a normalized event model <sup>feature · p0 · source</sup>
- [x] `0011` Filter session cruft out of the feed <sup>feature · p0 · source</sup>
- [x] `0012` Condense tool calls by rule, not by model <sup>feature · p0 · condense</sup>
- [x] `0013` Talk to any model over the OpenAI or Anthropic wire format <sup>feature · p0 · condense</sup>
- [x] `0014` Archive events and cache condensations in SQLite <sup>feature · p0 · store</sup>
- [x] `0015` Render a scrolling feed with expansion and filtering <sup>feature · p0 · ui</sup>
- [x] `0016` Ship sessions, export, doctor and config subcommands <sup>feature · p0 · cli</sup>
- [x] `0017` Write the README and licence <sup>feature · p0 · cli</sup>
- [x] `0052` Split a message's notes into alerts, details and links <sup>feature · p1 · condense</sup>
- [x] `0088` one way in <sup>chore · p0 · ui</sup>

## v0.2 — reads Codex too

`##########` 100% · 8 of 8 done · due 2026-09-28

The same feed for Codex sessions. One session list spanning both agents; you pick a session without caring which tool wrote it.

### done

- [x] `0018` How should a source report its read position? <sup>spike · p0 · source</sup>
- [x] `0019` Generalize the Source cursor beyond a byte offset <sup>chore · p0 · source</sup>
- [x] `0020` List Codex sessions alongside Claude Code's <sup>feature · p0 · source</sup>
- [x] `0021` Map Codex records onto the Event model <sup>feature · p0 · source</sup>
- [x] `0022` Check in Codex fixtures and test against them <sup>chore · p0 · source</sup>
- [x] `0023` Degrade instead of panicking on a bad transcript <sup>bug · p1 · source</sup>
- [x] `0024` Show which agent a session came from <sup>feature · p2 · ui</sup>
- [x] `0025` Install the scripts/task seam, git hooks and CI <sup>chore · p1 · dist</sup>

## v0.3 — one project, many agents

`##########` 100% · 17 of 17 done · due 2026-10-19

Browse every feed belonging to one project. Worktrees and parallel agents appear together, you switch between them, and you can search across the lot.

### done

- [x] `0026` Resolve a session's cwd to its repo root <sup>feature · p0 · source</sup>
- [x] `0027` Group the session picker by project <sup>feature · p0 · ui</sup>
- [x] `0028` Mark which of a project's sessions are live <sup>feature · p1 · ui</sup>
- [x] `0029` Switch between a project's feeds with one keystroke <sup>feature · p0 · ui</sup>
- [x] `0030` Interleave a project's sessions into one merged feed <sup>feature · p1 · ui</sup>
- [x] `0031` Search every stored session, not just the open one <sup>feature · p0 · store</sup>
- [x] `0032` Open a search result at its line <sup>feature · p1 · ui</sup>
- [x] `0054` Re-read sessions when the parser changes <sup>chore · p0 · store</sup>
- [x] `0055` Condense only what the reader is near <sup>feature · p0 · condense</sup>
- [x] `0057` Mark fresh messages and show Claude composing <sup>feature · p1 · ui</sup>
- [x] `0071` Harden the archive <sup>chore · p0 · store</sup>
- [x] `0072` Make condensation cheap enough not to think about <sup>chore · p0 · condense</sup>
- [x] `0073` Let several moans share one archive <sup>bug · p0 · store</sup>
- [x] `0074` Colour comes from a theme, and Claude is orange <sup>feature · p1 · ui</sup>
- [x] `0075` Decide how much history to keep <sup>feature · p1 · store</sup>
- [x] `0076` Read messages as markdown, and follow their links <sup>feature · p1 · ui</sup>
- [x] `0097` Never send a message that holds a credential <sup>feature · p0 · condense</sup>

## v0.4 — reads opencode

`##########` 100% · 5 of 5 done · due 2026-11-09

opencode sessions in the same feed as Claude Code and Codex. All three agents, one tool.

### done

- [x] `0033` Read opencode's SQLite store incrementally <sup>feature · p0 · source</sup>
- [x] `0034` Map opencode session, message and part rows onto the Event model <sup>feature · p0 · source</sup>
- [x] `0035` Never read a torn record while opencode is writing <sup>bug · p0 · source</sup>
- [x] `0036` Check in opencode fixtures and test against them <sup>chore · p1 · source</sup>
- [x] `0099` One unreadable source must not stop the rest <sup>bug · p0 · source</sup>

## v0.5 — the archive follows you

`#########·` 89% · 8 of 9 done · due 2026-12-14

The archive stops being a file on one machine. moan keeps its conversations in Turso — SQLite-compatible, pure Rust, no C compiler — and can optionally sync them, so the same history opens on your laptop and your desktop.

### in progress

- [ ] `0065` Sync an archive between your own machines <sup>feature · p0 · store</sup>

### done

- [x] `0059` How does an async store get called from the draw loop? <sup>spike · p0 · store</sup>
- [x] `0060` Put the store behind a trait <sup>chore · p0 · store</sup>
- [x] `0061` Port the schema and queries to Turso <sup>feature · p0 · store</sup>
- [x] `0062` Carry an existing SQLite archive over <sup>feature · p0 · store</sup>
- [x] `0063` Work with no account, no network, no config <sup>feature · p0 · store</sup>
- [x] `0064` What must never leave the machine? <sup>spike · p0 · store</sup>
- [x] `0066` Prove a broken sync cannot break the archive <sup>bug · p1 · store</sup>
- [x] `0067` Write down why the engine changed <sup>docs · p1 · store</sup>

## v0.6 — one window on everything

`##########` 100% · 10 of 10 done · due 2027-01-11

One window on everything. A sidebar you can open and close holds every session on the machine, grouped by project, marked live or waiting for you, with counts of what you have not read. It sits alongside the feed rather than on top of it, so you can read one project while watching another.

### done

- [x] `0078` Where does the keyboard go when there are two panes? <sup>spike · p0 · ui</sup>
- [x] `0079` Open and close a panel beside the feed <sup>feature · p0 · ui</sup>
- [x] `0080` List every session, grouped by project <sup>feature · p0 · ui</sup>
- [x] `0081` Say which sessions are waiting for a reply <sup>feature · p0 · ui</sup>
- [x] `0082` Count what has arrived since you last looked <sup>feature · p0 · store</sup>
- [x] `0083` Put search results in the panel <sup>feature · p0 · ui</sup>
- [x] `0084` Make the panel hold more than one thing <sup>chore · p1 · ui</sup>
- [x] `0085` Retire the modal picker <sup>chore · p1 · ui</sup>
- [x] `0087` Drive the panel with the mouse <sup>feature · p2 · ui</sup>
- [x] `0098` Make a hundred conversations browsable <sup>feature · p0 · ui</sup>

## v0.7 — one way in

`##########` 100% · 7 of 7 done · due 2027-02-08

One way in. moan grew three ways to move between conversations, two vocabularies for the same things, and chrome on every edge. This takes it back to one of each.

### done

- [x] `0090` One surface for switching, or does the rail earn its row? <sup>spike · p0 · ui</sup>
- [x] `0091` Say conversation, project and message <sup>docs · p0 · cli</sup>
- [x] `0092` Retire the rail <sup>chore · p0 · ui</sup>
- [x] `0093` Let the conversation be the interface <sup>feature · p1 · ui</sup>
- [x] `0094` Put me back where I was <sup>feature · p0 · ui</sup>
- [x] `0095` Stop narrating the machinery <sup>feature · p1 · ui</sup>
- [x] `0096` Make the empty states teach <sup>feature · p2 · ui</sup>

## v1.0 — the promise

`#########·` 89% · 8 of 9 done · due 2027-03-15

A terminal tool a stranger installs with one command on macOS or Linux, points at any of three coding agents, and reads. No new surface — this milestone is about being able to stand behind what is already there.

### in progress

- [ ] `0041` Tagged release with prebuilt binaries <sup>chore · p0 · dist</sup>

### done

- [x] `0037` Run the suite on macOS and Linux in CI <sup>chore · p0 · dist</sup>
- [x] `0038` Make the TUI behave on Linux terminals <sup>bug · p0 · ui</sup>
- [x] `0039` Version the database schema and migrate it forward <sup>chore · p0 · store</sup>
- [x] `0040` Give every user-facing error something to do about it <sup>chore · p0 · cli</sup>
- [x] `0042` Make --help, the README and the config reference agree <sup>docs · p1 · cli</sup>
- [x] `0043` Guard the feed's frame time against regression <sup>chore · p2 · ui</sup>
- [x] `0051` What breaks when moan runs on Linux? <sup>spike · p0 · ui</sup>
- [x] `0053` Stale SQLite sidecars abort startup with a disk I/O error <sup>bug · p1 · store</sup>

## later — later

`··········` 0% · 0 of 9 done

Good ideas without a date yet.

### backlog

- [ ] `0044` Watch two feeds side by side in split panes <sup>feature · p3 · ui</sup>
- [ ] `0045` Generate shell completions <sup>feature · p3 · cli</sup>
- [ ] `0046` Add a fourth agent <sup>feature · p3 · source</sup>
- [ ] `0047` Override the model per session or per project <sup>feature · p3 · condense</sup>
- [ ] `0048` Make the palette themeable <sup>feature · p3 · ui</sup>
- [ ] `0049` Notify when a session goes idle or asks a question <sup>feature · p3 · ui</sup>
- [ ] `0050` Replay a session at speed <sup>feature · p3 · ui</sup>
- [ ] `0056` Collapse a project in the session picker <sup>feature · p3 · ui</sup>
- [ ] `0070` Hold less of a session in memory <sup>chore · p3 · store</sup>

