# Pinned so a build is the same everywhere, and so a toolchain upgrade is a
# change somebody made on purpose rather than something that happened.
[toolchain]
channel = "1.98.1"
components = ["rustfmt", "clippy"]
